[uwsgi]
project = orthosie
home-dir = /home/vagrant
base = /vagrant

chdir = %(base)/%(project)
home = %(home-dir)/Env/%(project)
module = %(project)/%(project).wsgi:application

master = true
processes = 5

socket = %(base)/%(project).sock
chmod-socket = 664
vacuum = true
