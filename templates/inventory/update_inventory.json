{
  "upc": "{{ item.upc }}",
  "name": "{{ item.name }}",
  "price": {{ item.price }},
  "scalable": {% if item.scalable %}true{% else %}false{% endif %},
  "taxable": {% if item.taxable %}true{% else %}false{% endif %},
  "vendor_name": "{{ item.vendor.name }}"
}
